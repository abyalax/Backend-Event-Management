meta {
  name: Get Transaction by ID
  type: http
  seq: 5
}

get {
  url: "{{URL_API}}/payments/transactions/{{transactionId}}"
  body: none
  auth: bearer
}

headers {
  Authorization: Bearer {{authToken}}
}
