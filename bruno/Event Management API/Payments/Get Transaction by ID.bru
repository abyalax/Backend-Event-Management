meta {
  name: Get Transaction by ID
  type: http
  seq: 5
}

get {
  url: "{{base_url}}/payments/transactions/{{transactionId}}"
  body: none
  auth: bearer
}

headers {
  Authorization: Bearer {{authToken}}
}
